'use client'

import React, { useEffect, useRef, useState } from 'react'

// ChiffresSection is now merged into HeroSection's stats bar.
// This component renders nothing — kept for backward compatibility.
export default function ChiffresSection() {
  return null
}
